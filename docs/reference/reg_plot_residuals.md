# Residual Diagnostics

Returns a multi-panel diagnostic plot from a fitted regression model.

## Usage

``` r
reg_plot_residuals(model_result)
```

## Arguments

- model_result:

  A list returned by any `reg_*()` function.

## Value

A ggplot-based composite.
