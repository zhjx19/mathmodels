% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interp_fit.R
\name{growth_fit}
\alias{growth_fit}
\title{Nonlinear Growth Curve Fit}
\usage{
growth_fit(x, y, type = c("logistic", "gompertz", "saturation", "mm"))
}
\arguments{
\item{x}{Numeric vector of observed predictor values.}

\item{y}{Numeric vector of observed response values.}

\item{type}{Character, one of \code{"logistic"}, \code{"gompertz"}, \code{"saturation"},
or \code{"mm"}.}
}
\value{
A named list, see \code{\link[=poly_fit]{poly_fit()}} for details. \code{model} contains
the \code{nls} object.
}
\description{
Fits nonlinear growth, saturation, and sigmoid curves via
\code{minpack.lm::nlsLM()} with automatic starting value generation.
}
\details{
Models and starting value strategies:

\strong{Logistic}: \code{y = L / (1 + exp(-k * (x - x0)))}
\itemize{
\item \code{L0 = max(y) * 1.05}
\item \code{lm(log((L0 - y) / y) ~ x)} for \verb{k, x0} initials
}

\strong{Gompertz}: \code{y = L * exp(-exp(-k * (x - x0)))}
\itemize{
\item \code{L0 = max(y) * 1.05}
\item \code{lm(log(-log(y / L0)) ~ x)} for \verb{k, x0} initials
}

\strong{Saturation} (exponential approach): \code{y = a * (1 - exp(-b * x))}
\itemize{
\item \code{a0 = max(y) * 1.1}
\item \code{lm(log(a0 - y) ~ x)} for \code{b} initial
}

\strong{Michaelis-Menten}: \code{y = a * x / (b + x)}
\itemize{
\item \code{a0 = max(y) * 1.1}
\item Lineweaver-Burk \code{lm(1/y ~ 1/x)} for \verb{a, b} initials
}

Falls back to heuristic defaults if linearized estimation fails.
}
\examples{
\dontrun{
set.seed(42)
x = seq(0, 10, length.out = 30)
y = 100 / (1 + exp(-1.5 * (x - 5))) + rnorm(30, sd = 2)
growth_fit(x, y, type = "logistic")
}

}
