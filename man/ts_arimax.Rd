% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pred_ts.R
\name{ts_arimax}
\alias{ts_arimax}
\title{ARIMAX Model Fitting}
\usage{
ts_arimax(
  x,
  xreg,
  order = NULL,
  seasonal = NULL,
  stepwise = TRUE,
  approximation = TRUE,
  ...
)
}
\arguments{
\item{x}{A complete \code{ts_df} with no missing values.}

\item{xreg}{A matrix or data.frame of exogenous regressors with
\code{nrow(xreg) == nrow(x)}.}

\item{order}{Optional non-seasonal ARIMA order. If \code{NULL} (default),
automatic order selection via \code{forecast::auto.arima()}.}

\item{seasonal}{Optional seasonal specification.}

\item{stepwise, approximation}{Passed to \code{forecast::auto.arima()}.}

\item{...}{Additional arguments passed to the forecast fitting function.}
}
\value{
A list containing tidy model summaries, the raw model, input,
and the exogenous regressors used.
}
\description{
Fits an ARIMA model with exogenous regressors (ARIMAX).
}
\examples{
x = as_ts_df(log(AirPassengers))
xreg = data.frame(trend = seq_len(nrow(x)))
ts_arimax(x, xreg = xreg)
}
