% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epidemic_plot.R
\name{compute_incidence}
\alias{compute_incidence}
\title{Compute incidence from ODE output}
\usage{
compute_incidence(
  df,
  infection_col = "I",
  death_col = "D",
  population_col = "S"
)
}
\arguments{
\item{df}{A data frame with a \code{time} column and
compartment columns.}

\item{infection_col}{Name of the infectious compartment column
(default \code{"I"}).}

\item{death_col}{Name of the deceased compartment column
(default \code{"D"}).  If the column does not exist, death
incidence is silently skipped.}

\item{population_col}{Name of the susceptible compartment column
(default \code{"S"}).  The negative difference of \code{S} is used as
a second estimate of new infections.}
}
\value{
A data frame with columns \code{time}, \code{new_infection}
(from S depletion), \code{new_infection_I} (from I change), and
\code{new_death} (if \code{death_col} exists).  The first row of each
difference column is \code{NA}.
}
\description{
Estimates daily new infections and new deaths from the change in
compartment sizes between consecutive time steps.
}
\examples{
sir = model_sir(
  init   = c(S = 990, I = 10),
  params = c(beta = 0.002, gamma = 0.1),
  times  = seq(0, 50, by = 0.1)
)
inc = compute_incidence(sir)
head(inc)

}
