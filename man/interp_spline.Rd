% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interp_fit.R
\name{interp_spline}
\alias{interp_spline}
\title{Cubic Spline Interpolation}
\usage{
interp_spline(x, y, xout, spar = NULL)
}
\arguments{
\item{x}{Numeric vector of observed x-values (no duplicates, length >= 4).}

\item{y}{Numeric vector of observed y-values.}

\item{xout}{Numeric vector of x-values where interpolation is desired.}

\item{spar}{Smoothing parameter passed to \code{smooth.spline()}. \code{NULL} for
standard (non-smoothing) spline.}
}
\value{
A tibble with columns \code{x} and \code{y}. The attribute \code{method} is set to
\code{"spline"} or \code{"smooth_spline"} depending on the variant used.
}
\description{
Performs cubic spline interpolation. When \code{spar} is \code{NULL}, uses
\code{stats::spline()} with the FMM method. Otherwise uses
\code{stats::smooth.spline()} for smoothing.
}
\examples{
x = c(1, 2, 3, 4, 5)
y = c(2, 4, 1, 5, 3)
interp_spline(x, y, xout = c(1.5, 2.5, 3.5))
interp_spline(x, y, xout = c(1.5, 2.5, 3.5), spar = 0.8)

}
