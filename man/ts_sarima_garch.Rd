% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pred_ts.R
\name{ts_sarima_garch}
\alias{ts_sarima_garch}
\title{Two-Stage SARIMA-GARCH Joint Model}
\usage{
ts_sarima_garch(
  x,
  sarima_order = NULL,
  sarima_seasonal = NULL,
  garch_order = c(1, 1),
  garch_dist = "std",
  frequency = NULL,
  ...
)
}
\arguments{
\item{x}{A numeric vector or \code{ts} object.}

\item{sarima_order}{Integer vector \code{c(p,d,q)} or \code{NULL} (auto).}

\item{sarima_seasonal}{List \code{list(order=c(P,D,Q), period=m)} or \code{NULL}.}

\item{garch_order}{Integer vector \code{c(p,q)} (default \code{c(1,1)}).}

\item{garch_dist}{Character. Innovation distribution for GARCH (default
\code{"std"}).}

\item{frequency}{Integer. Required for plain numeric \code{x}.}

\item{...}{Additional arguments forwarded to \code{ts_sarima()}.}
}
\value{
A named list:
\describe{
\item{mean_model}{Result list from \code{ts_sarima()}.}
\item{variance_model}{Result list from \code{ts_garch()}.}
\item{fitted}{Combined tibble: \code{index, observed, mean_fitted,
      sigma, variance, std_residual}.}
\item{model_info}{Combined one-row summary tibble.}
}
}
\description{
Stage 1: fits a SARIMA model on the level series via \code{ts_sarima()}.
Stage 2: fits a GARCH model on the SARIMA residuals via \code{ts_garch()}.
Returns both sub-models plus a combined fitted tibble.
}
\examples{
data(AirPassengers)
res = ts_sarima_garch(log(AirPassengers))
res$model_info

}
