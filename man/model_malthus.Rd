% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffEq_models.R
\name{model_malthus}
\alias{model_malthus}
\title{Malthusian (Exponential) Growth Model}
\usage{
model_malthus(init, params, times, ...)
}
\arguments{
\item{init}{Named numeric vector, e.g. \code{c(N = 100)}.}

\item{params}{Named numeric vector, e.g. \code{c(r = 0.3)}.
\describe{
\item{r}{Intrinsic growth rate (per time unit).}
}}

\item{times}{Numeric vector of output times.}

\item{...}{Additional arguments passed to \code{ode_solver}.}
}
\value{
A \code{data.frame} with columns \code{time} and \code{N}.
}
\description{
Solves the Malthusian growth equation:
\deqn{\frac{dN}{dt} = r \, N}
}
\details{
The analytical solution is \eqn{N(t) = N_0 \, e^{r t}}, which can be used
to verify numerical accuracy.
}
\examples{
res = model_malthus(
  init   = c(N = 100),
  params = c(r = 0.3),
  times  = seq(0, 10, by = 0.1)
)
# Compare with analytical solution
res$N_exact = 100 * exp(0.3 * res$time)
head(res)

}
