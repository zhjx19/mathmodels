% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pred_reg.R
\name{reg_logistic}
\alias{reg_logistic}
\title{Logistic Regression}
\usage{
reg_logistic(
  formula,
  data,
  step = FALSE,
  direction = c("both", "forward", "backward"),
  ...
)
}
\arguments{
\item{formula}{A formula, e.g. \code{y ~ x1 + x2}.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{step}{Logical. Whether to perform stepwise selection (\code{TRUE}/\code{FALSE}).
Default is \code{FALSE}.}

\item{direction}{Character: \code{"both"} (default), \code{"forward"}, or \code{"backward"}.}

\item{...}{Additional arguments passed to \code{lm()} and \code{step()}.}
}
\value{
A named list analogous to \code{reg_lm()}, plus:
\describe{
\item{odds_ratio}{Tibble with odds ratios and 95\% CIs.}
\item{residuals}{DataFrame of observed, fitted probability, residual.}
}
}
\description{
Fits a binary logistic regression model via \code{glm(family = binomial())},
with optional stepwise selection.
}
\details{
Same stepwise logic as \code{\link{reg_lm}}, but uses GLM with the binomial family.
A Hosmer-Lemeshow goodness-of-fit test is included in diagnostics.
}
\examples{
set.seed(42)
x1 = rnorm(100)
logit = 0.5 + x1
prob = 1 / (1 + exp(-logit))
y = rbinom(100, 1, prob)
reg_logistic(y ~ x1, data = data.frame(y, x1))

}
