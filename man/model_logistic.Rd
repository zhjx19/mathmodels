% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffEq_models.R
\name{model_logistic}
\alias{model_logistic}
\title{Logistic Population Growth Model}
\usage{
model_logistic(init, params, times, ...)
}
\arguments{
\item{init}{Named numeric vector, e.g. \code{c(N = 10)}.}

\item{params}{Named numeric vector, e.g. \code{c(r = 0.5, K = 100)}.
\describe{
\item{r}{Intrinsic growth rate.}
\item{K}{Carrying capacity.}
}}

\item{times}{Numeric vector of output times.}

\item{...}{Additional arguments passed to \code{ode_solver}.}
}
\value{
A \code{data.frame} with columns \code{time} and \code{N}.
}
\description{
Solves the logistic growth equation:
\deqn{\frac{dN}{dt} = r \left(1 - \frac{N}{K}\right) N}
}
\details{
The analytical solution is
\eqn{N(t) = K \,/\, \bigl(1 + (K/N_0 - 1)\,e^{-r t}\bigr)},
which can be used to verify numerical accuracy.
}
\examples{
res = model_logistic(
  init   = c(N = 10),
  params = c(r = 0.5, K = 100),
  times  = seq(0, 20, by = 0.1)
)
head(res)

}
